{
  "name": "latex-service",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone XeLaTeX compile service for ATS Resume AI. Renders resume JSON to LaTeX and compiles it to PDF using a real TeX Live install, since Vercel's serverless functions can't run xelatex directly.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "express": "^4.21.2",
    "handlebars": "^4.7.9",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
